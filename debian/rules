#!/usr/bin/make -f
%:
	dh $@ --sourcedirectory=dependencies
	dh $@ --sourcedirectory=.

override_dh_auto_configure:
	dh_auto_configure -- -O--sourcedirectory=dependencies -DCMAKE_INSTALL_PREFIX=./dist
